Run services from docker-compose - zookeeper, kafka, kafka-ui.
or
Open terminal and enter:

    docker-compose -f local-dev.yml up

# Kafka

This applications needs the following Kafka topics:

* library-events
